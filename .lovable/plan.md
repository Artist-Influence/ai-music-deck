

## Slide 4 Label Fix

**Changes in `WhatCloutedDoesSlide.tsx`:**

1. **Line 103**: Increase label font from `text-lg` (18px) to `text-[21px]` (~15% bump)
2. **Line 102**: Change `items-start` to `items-center` on the GlassPanel so the labels vertically center within each row

