

## Slide 6 (ClippingSlide) Updates

### Changes (all in `src/components/deck/slides/ClippingSlide.tsx`)

1. **Title**: Change "Clipping" to `Clipping Distribution`

2. **Subtitle**: Change to `A distribution network powered by 20,000+ clippers creating across TikTok, Instagram, and Youtube.`

3. **Remove** the "What it is" GlassPanel entirely.

4. **Change layout** from `grid-cols-3` to `grid-cols-2` (since we now have 2 panels).

5. **First panel — "Pros"** (replaces "What you get"):
   - Header: `Pros`
   - Bullet points:
     - "Target viral trends with a strategic campaign brief"
     - "Fast velocity and clear reporting"
     - "UGC reuse rights for paid and owned channels"

6. **Second panel — "How it works"**: Keep existing content (launch in 24hrs, runs 7-14 days, official sound/tags) plus the pricing grid, and **add** the timeframe line ("Timeframe: 2 to 4 weeks end-to-end") at the bottom of this panel.

