

## Slide 4 (WhatCloutedDoesSlide) Updates

### Changes (all in `src/components/deck/slides/WhatCloutedDoesSlide.tsx`)

1. **Title**: Replace with `Coordinated distribution so your music and artists build momentum like a system, not a gamble.`

2. **Remove** the subtitle paragraph (line 96-98) that currently says "Clouted coordinates distribution across…". Integrate "algorithm" language into the "We do three things well" subheader — change it to something like: `Three pillars that work the algorithm for you`

3. **Update the three items**:
   - `Volume` → "Flood social platforms with high-velocity short-form content"
   - `Curation` → "Target real communities with reach within your niche"
   - `Amplification` → "Amplify winners with paid spend once the creative proves itself"

All changes in copy only — no structural or visual changes needed.

