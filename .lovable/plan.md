

## Two Slide Fixes

### Slide 7 (FanpagesSlide.tsx)
- Line 56: Change `h-32` to `h-40` and remove `opacity-60`, making the visualizer span the full width of the "What it is" panel with more vertical space.

### Slide 4 (WhatCloutedDoesSlide.tsx)
- Line 102: Increase padding from `p-5` to `p-6`, increase gap from `gap-4` to `gap-6`, and widen the label column from `w-32` to `w-40` (~25% wider) to prevent overlap between title and description.

